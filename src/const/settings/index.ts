import { DEFAULT_COMMON_SETTINGS } from "@/const/settings/common";
import { UserSettings } from "@/types/user/settings";

export const DEFAULT_SETTINGS: UserSettings = {
  common: DEFAULT_COMMON_SETTINGS,
};
